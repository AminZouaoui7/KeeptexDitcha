# Configuration pour le PC B qui se connecte à PostgreSQL sur le PC A
# Copiez ce fichier vers .env et modifiez les valeurs selon votre configuration

# Configuration du serveur
PORT=5000
HOST=0.0.0.0
NODE_ENV=development

# Configuration PostgreSQL - Connexion à distance
# Remplacez 192.168.1.X par l'adresse IP réelle du PC A
POSTGRES_HOST=192.168.1.X
POSTGRES_PORT=5433
POSTGRES_DB=keeptex
POSTGRES_USER=postgres
POSTGRES_PASSWORD=1234

# URI de connexion complète pour PostgreSQL
# Utilisé directement par Sequelize
POSTGRES_URI=postgres://postgres:1234@192.168.1.X:5433/keeptex

# Configuration MongoDB (si utilisé)
MONGO_URI=mongodb://localhost:27017/keeptex

# Clé secrète pour JWT
JWT_SECRET=keep_tex_secret_key_2023

# Configuration SMTP pour l'envoi d'emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_EMAIL=votre-email@gmail.com
SMTP_PASSWORD=votre-mot-de-passe-app
FROM_NAME=keeptex
FROM_EMAIL=votre-email@gmail.com